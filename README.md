# Mini-lab Firewall Linux

## Objectif
Configurer un firewall simple avec iptables pour autoriser SSH, HTTP, HTTPS et bloquer le reste.

## Contenu
- `firewall.sh` : script de configuration du firewall

## Instructions
1. Cloner le repo :
   git clone <URL_DU_REPO>
2. Exécuter le script :
   sudo bash firewall.sh
3. Tester avec :
   ping <IP>
   curl http://localhost
